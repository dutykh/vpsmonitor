# Website Monitor - runtime dependencies
# Author: Dr. Denys Dutykh (Khalifa University of Science and Technology, Abu Dhabi, UAE)
#
# Production install:  pip install -r requirements.txt
# Everything else the monitor needs is in the Python standard library
# (sqlite3, ssl, tomllib, concurrent.futures, fcntl, smtplib, gzip).

requests>=2.31.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0
